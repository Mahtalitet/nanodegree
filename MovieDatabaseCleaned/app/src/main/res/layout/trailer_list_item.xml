<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/cDarkBlue"
    android:paddingLeft="@dimen/detail_fragment_overview_margin"
    >
    
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_action_view_trailer"
        android:scaleType="fitCenter"
        android:id = "@+id/trailer_item_image"
        android:minHeight="48dp"

        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:textColor="@color/cWhite"
        android:text="It should be a trailer here."
        android:textStyle="normal"
        android:fontFamily="monospace"
        android:gravity="center_vertical"
        android:id="@+id/trailer_item_text"


        />

</LinearLayout>